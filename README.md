Вот чистая Markdown-разметка для `README.md` — полностью готовая к размещению на GitHub:


# 🗺️ Тестовое задание SquareGPS

## 📖 Описание
Одностраничное приложение (**SPA**) на **Vue 2 + TypeScript + Vuex + Vue Router + Vuetify**, состоящее из двух разделов:
- **О задании** — отображает текст тестового задания;
- **Карта** — интерактивная карта с маркерами, списком точек, локальным хранением данных и поддержкой локализации (RU/EN).



## ⚙️ Основные возможности
- Добавление маркеров кликом по карте или через поиск по адресу.
- Обратное геокодирование через [geocode.maps.co](https://geocode.maps.co) с обработкой ошибок и таймаутов.
- Подсветка записи списка при клике по маркеру; центрирование карты при клике по записи.
- Локальное хранение (`localStorage`) через класс `Backend<T>`.
- Синхронизация выбранного маркера с URL: `/map/:id`.
- Локализация (RU/EN), все строки вынесены в JSON.
- Адаптивная вёрстка для мобильных устройств (Drawer + кнопка показа списка).
- 1 модульный тест (Jest) на компонент `MarkerList.vue`.



## 🧰 Технологический стек

| Категория | Технологии |
|------------|-------------|
| Фреймворк | Vue 2.6 + TypeScript 4.5 |
| Управление состоянием | Vuex 3 |
| Роутинг | Vue Router 3 |
| UI | Vuetify 2 |
| Карта | Leaflet |
| Локализация | vue-i18n v8 |
| Инфраструктура | portal-vue |
| Тестирование | Jest + @vue/test-utils |



## 🧩 Структура проекта

```bash
src/
  components/
    MapCanvas.vue      # Карта (Leaflet), добавление маркеров
    MarkerList.vue     # Список маркеров
    MarkerTooltip.vue  # Попап маркера (локализация)
locales/
  ru.json
  en.json
router/
  index.ts
services/
  GeoService.ts    # Обратное геокодирование
  markerBackend.ts # Эмуляция хранения в localStorage
store/
  index.ts
views/
  AboutPage.vue 
  MapView.vue
App.vue
i18n.ts
main.ts
tests/
  setup.ts
unit/
  MarkerList.spec.ts
  ```


## ⚙️ Установка и запуск

### 1. Клонировать репозиторий
```bash
git clone https://github.com/AlexanderJarvinen/square_gps_test_task_map_app
cd squaregps-test-app
````

### 2. Установить зависимости

```bash
npm install
```

### 3. (Необязательно) Добавить `.env.local`

```bash
VUE_APP_GEOCODE_API_KEY=your_api_key_here
```

### 4. Запустить dev-сервер

```bash
npm run serve
```

Откройте [http://localhost:8080](http://localhost:8080)

### 5. Сборка проекта

```bash
npm run build
```

### 6. Проверка линтером

```bash
npm run lint
```

### 7. Запуск модульных тестов

```bash
npm run test:unit
```

---

## 🌍 Локализация

* Файлы: `src/locales/ru.json`, `src/locales/en.json`
* Используется `vue-i18n@8`
* Переключение языков реализовано в `App.vue`

Пример инициализации i18n для динамического компонента:

```ts
const vm = new (Vue.extend(MarkerTooltip))({
  i18n: this.$i18n,
  propsData: { marker: m, lat: m.lat, lng: m.lng },
}).$mount(div);
```

---

## 🧪 Тестирование (Jest)

* Конфигурация: `jest.config.js`
* Глобальный setup: `tests/setup.ts`
  включает моки `$t`, `$tc`, `$i18n` и контейнер `data-app` для Vuetify

Пример теста (`tests/unit/MarkerList.spec.ts`):

* проверяет эмит события `select`;
* проверяет корректный рендер заголовка и координат;
* проверяет удаление всех маркеров при `clear-all`.

---

## 🧱 Настройки TypeScript

* `"resolveJsonModule": true` — для импорта JSON локалей.
* Шимы (`src/shims-vue.d.ts`) расширяют тип `Vue`:

```ts
interface Vue {
  $store: Store<RootState>;
  $vuetify: Framework;
  $router: VueRouter;
  $route: Route;
  $t(key: string, ...args: any[]): string;
  $i18n: IVueI18n;
}
```


## ✅ Соответствие требованиям задания

| Требование                       | Статус |
| -------------------------------- | ------ |
| Vue.js, Vuex, VueRouter, Vuetify | ✅      |
| Хедер с 2 пунктами меню          | ✅      |
| Раздел “О задании”               | ✅      |
| Раздел “Карта”                   | ✅      |
| Добавление и хранение маркеров   | ✅      |
| Отображение id в URL             | ✅      |
| Мобильная версия                 | ✅      |
| Локализация RU/EN                | ✅      |
| 1 модульный тест (Jest)          | ✅      |


## 📜 Лицензия

MIT (можно изменить при необходимости)


